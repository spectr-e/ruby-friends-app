<%= form_with(model: friend) do |form| %>
  <% if friend.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(friend.errors.count, "error") %> prohibited this friend from being saved:</h2>

      <ul>
        <% friend.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <%= form.text_field :first_name, class:"form-control col-5", placeholder:"First Name"%>
    </div>

    <div class="form-group">
      <%= form.text_field :last_name, class:"form-control col-5", placeholder:"Last Name"%>
    </div>

    <div class="form-group">
      <%= form.text_field :email, class:"form-control col-5", placeholder:"Email"%>
    </div>

    <div class="form-group">
      <%= form.text_field :phone, class:"form-control col-5", placeholder:"Phone"%>
    </div>

    <div class="form-group">
      <%= form.text_field :twitter, class:"form-control col-5", placeholder:"Twitter"%>
    </div>

    <div class="form-group">
      <%= form.submit class:"btn btn-secondary" %>
    </div>
<% end %>
